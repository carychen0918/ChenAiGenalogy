<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>chen.genealogy</groupId>
        <artifactId>chen-module-genealogy</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>chen-module-genealogy-server</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>  <!-- 5. 新增依赖，这里引入的都是比较常用的业务组件、技术组件 -->
        <!-- Spring Cloud 基础 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-env</artifactId>
        </dependency>

        <!-- 依赖服务 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-module-system-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-module-infra-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-module-genealogy-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-biz-data-permission</artifactId>
        </dependency>
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-biz-tenant</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-redis</artifactId>
        </dependency>

        <!-- RPC 远程调用相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-rpc</artifactId>
        </dependency>

        <!-- Registry 注册中心相关 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <!-- Config 配置中心相关 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>

        <!-- Job 定时任务相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-job</artifactId>
        </dependency>

        <!-- 消息队列相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-mq</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-test</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-excel</artifactId>
        </dependency>
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-biz-ip</artifactId>
        </dependency>
        <dependency>
            <groupId>com.belerweb</groupId>
            <artifactId>pinyin4j</artifactId>
        </dependency>

        <!-- 监控相关 -->
        <dependency>
            <groupId>chen.genealogy</groupId>
            <artifactId>chen-spring-boot-starter-monitor</artifactId>
        </dependency>
    </dependencies>

    <build>
        <!-- 设置构建的 jar 包名 -->
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <!-- 打包 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal> <!-- 将引入的 jar 打入其中 -->
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
